version: 1
frontend:
  phases:
    build:
      commands:
        - rm -rf /root/.nvm/default-packages
        - nvm install 15
        - yarn version
        - npm run build
        - which node
        - amplify --version
        - which amplify
  artifacts:
    files:
        - index.html
    baseDirectory: src
