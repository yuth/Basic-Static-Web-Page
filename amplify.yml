version: 1
frontend:
  phases:
    build:
      commands:
        - npm run build
        - which node
        - amplify --version
        - which amplify
  artifacts:
    files:
        - index.html
    baseDirectory: src
