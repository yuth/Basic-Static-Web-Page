version: 1
frontend:
  phases:
    build:
      commands:
        - nvm install 14.21.1
        - yarn version
        - which yarn
        - npm run build
        - which node
        - amplify --version
        - which amplify
  artifacts:
    files:
        - index.html
    baseDirectory: src
