version: 1
frontend:
  phases:
    build:
      commands:
        - npm run build
        - amplify --version
        - which amplify
  artifacts:
    files:
        - index.html
    baseDirectory: src
